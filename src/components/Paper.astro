<div id="paper">
  <div id="paper-dots"></div>
  <div id="paper-content">
    <slot></slot>
  </div>
</div>

<style>
  #paper {
    position: relative;
    margin-left: 20vw;
    margin-top: 20vh;
    width: 60vw;
    min-height: 80vh;
    background-color: #f4e9c9;
    border-top-left-radius: 16px;
    border-top-right-radius: 16px;
    overflow: hidden;
  }

  #paper-dots {
    position: absolute;
    inset: 32px 32px 0 32px;
    background-image: radial-gradient(circle, rgba(0, 0, 0, 0.22) 1.4px, transparent 1.4px);
    background-size: 26px 26px;
    pointer-events: none;
  }

  #paper-content {
    inset: 18px 44px;
    position: relative;
    color: #1a1a1a;
  }
</style>
